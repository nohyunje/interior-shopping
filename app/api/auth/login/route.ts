import { NextResponse } from 'next/server';import { createSession,sessionCookie,verifyAdminCredentials } from '@/lib/auth';
export async function POST(req:Request){try{const {id,password}=await req.json();if(typeof id!=='string'||typeof password!=='string'||!verifyAdminCredentials(id,password))return NextResponse.json({error:'아이디 또는 비밀번호가 올바르지 않습니다.'},{status:401});const res=NextResponse.json({ok:true});res.cookies.set(sessionCookie.name,createSession(),sessionCookie.options);return res}catch(error){console.error('Admin login configuration error',error);return NextResponse.json({error:'관리자 인증 환경변수가 올바르게 설정되지 않았습니다.'},{status:503})}}
