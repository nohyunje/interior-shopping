'use client';import { useState } from 'react';import { Lock,ArrowRight } from 'lucide-react';
export default function Login(){const [id,setId]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,password})});setBusy(false);if(r.ok)location.reload();else setError((await r.json()).error)}return <div className="login-page"><a className="logo" href="/">ROOM VIEW<span>.</span></a><form onSubmit={submit}><i><Lock/></i><span>OWNER ACCESS</span><h1>관리자 로그인</h1><p>승인된 관리자만 접근할 수 있습니다.</p><label>아이디<input autoFocus value={id} onChange={e=>setId(e.target.value)} required autoComplete="username"/></label><label>비밀번호<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></label>{error&&<em>{error}</em>}<button disabled={busy}>{busy?'확인 중...':'로그인'} <ArrowRight/></button><a href="/">사이트로 돌아가기</a></form></div>}
